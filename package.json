{
  "name": "superposition-devtools",
  "private": true,
  "type": "module",
  "description": "Dev tooling only (budget script, Playwright). The site itself has no build step and no runtime dependencies.",
  "scripts": {
    "budget": "node tools/budget.mjs",
    "test": "playwright test"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@fontsource-variable/archivo": "5.2.8",
    "@fontsource/fragment-mono": "5.2.8",
    "@playwright/test": "1.61.1"
  }
}
